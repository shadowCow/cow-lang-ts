import { test } from "node:test";
import assert from "node:assert";

test("greet function", { todo: true }, () => {
  const actual = "what";
  const expected = "what";

  assert.strictEqual(actual, expected);
});
